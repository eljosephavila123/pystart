PyStart 

The function that implements this algorithm receives the coordinates of an origin position,
a destination position, the metro map of a city and the type of criterion that we want to optimize represented 
by a number (type_preference) and searches for the optimal path between the colon using the A * search.



